// WAP to bubble sort array the user enters

#include <stdio.h>
int main(){
    int a_len;
    printf("Enter length: ");
    scanf("%d",&a_len);
    printf("Enter array");
    int a[a_len]
    for(int i=0;i<a_len;i++){
        
    }
    return 0;
}